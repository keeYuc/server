#include<string.h>
//#include<openssl/rsa.h>
#include<iostream>
#include<stdio.h>

int main()
{
    printf("s");
    // auto rsa = RSA_new();
    // auto num = BN_new();
    // printf("s");
    // RSA_generate_key_ex(rsa, 1024, num, nullptr);
    // printf("%s\n", (char*) RSAPublicKey_dup(rsa));
    // printf("%s\n", (char*) RSAPrivateKey_dup(rsa));

    // RSA_free(rsa);

    return 0;
}